{% comment %}
  SSR placeholder for Lucide icons — hydrated via initLucideIcons (data-lucide).
  @param name {string} Lucide kebab-case icon name (required)
  @param class {string} optional CSS classes on the placeholder element
{% endcomment %}
<i data-lucide="{{ name }}" class="{{ class }}" aria-hidden="true"></i>
